import { CodeBlockRuntime } from "@rspress/core/theme"
import { transformerNotationHighlight } from "@shikijs/transformers"

### Function
```ts
const hydra = (options: PluginOptions): RsbuildPlugin => {}
```

### Options
```ts
interface PluginOptions {
	/**
	 * Path to Hono api routes folder
	 */
	apiDirectory: string

	/**
	 * Where the file containing the Hono routes will be placed
	 * */
	routesFile: string
}
```

### Examples
```text
root/
├── src/
│   ├── api/
│   │   ├── route01.ts
│   │   └── route02.ts
├── api-routes.gen.ts
├── rsbuild.config.ts
└── package.json
```

<CodeBlockRuntime
	lang="ts"
	title="rsbuild.config.ts"
	shikiOptions={{
		transformers: [ transformerNotationHighlight() ],
	}}
	code={`hydra({
		apiDirectory: " src/api", // [!code highlight]
		routesFile: " src/api-routes.gen.ts", // [!code highlight]
	})`}
/>